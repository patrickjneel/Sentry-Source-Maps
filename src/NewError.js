import React, { Component } from 'react';

class NewError extends Component {

  componentDidMount() {
    const arr = [];
    arr.filter(iterm => iterm.id)
  }

    render() {
      return (
         <div>
        Hello 
      </div>
        )
     
    }
  }

  export default NewError;